import React, { FC } from "react";

import "./Nav.css";

const TopBar: FC = () => {

    return(
        <nav className="topBar">
            Bottom Nav
        </nav>
    );
}

export default TopBar;
